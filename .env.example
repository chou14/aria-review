# Aria Review environment example.
# Copy to .env for Docker Compose. Do not commit real keys.

# Required user configuration
# None. The zero-key demo and default workbench run with deterministic/offline fallbacks.

# Optional external service keys and endpoints
OCR_AUTHORIZATION_TOKEN=
MINERU_BASE_URL=https://mineru.net/api/v4
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
# Reject localhost, private IPs, and .local Base URLs by default; set to 1 only for intentional internal LLM/API endpoints.
BIBLIOCN_ALLOW_PRIVATE_API_BASE_URLS=
SCIVERSE_API_TOKEN=
SCIVERSE_BASE_URL=https://api.sciverse.space
IMAGE_API_KEY=
IMAGE_BASE_URL=https://api.openai.com/v1
IMAGE_MODEL=gpt-image-1
IMAGE_SIZE=1024x1024
OPENALEX_EMAIL=

# Agent and Docker Compose runtime
R_ANALYSIS_URL=http://r-analysis:8001
CORS_ORIGINS=http://localhost:8080
DATABASE_URL=postgresql+asyncpg://bibliocn:bibliocn@postgres:5432/bibliocn
TEST_DATABASE_URL=postgresql+asyncpg://bibliocn:bibliocn@postgres:5432/bibliocn_test
BIBLIOCN_CORPORA_DIR=/data/corpora
POSTGRES_PORT=55432

# Tuning and limits
R_REQUEST_TIMEOUT=120
R_INGEST_TIMEOUT=300
R_HEALTH_TIMEOUT=5
MAX_UPLOAD_BYTES=52428800
REVIEW_RECORDS_LIMIT=40
SCIVERSE_TIMEOUT=60
SCIVERSE_CONTENT_CHUNK_CHARS=7000
SCIVERSE_CONTENT_MAX_CHARS=500000
